# Prazno = relativni pozivi (/api/...), za produkciju gde backend sam
# servira i frontend dist/ fajlove (isti origin/port). Pun URL
# (https://host:3000) samo za lokalni dev gde frontend i backend rade na
# različitim portovima (Vite dev server + Express).
VITE_API_URL=
VITE_HOST_IP_ADDRESS=
VITE_HOST_PORT=
VITE_SSL_KEY_PATH=
VITE_SSL_CERT_PATH=